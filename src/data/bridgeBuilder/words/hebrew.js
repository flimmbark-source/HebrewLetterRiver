/**
 * Hebrew Bridge Builder words — re-exports the original data
 * with language-agnostic fields already applied.
 */
export { bridgeBuilderWords as bridgeBuilderWordsHebrew } from '../../bridgeBuilderWords.js';
